project: ${PROJECT}
namespaceTag: ${NAMESPACETAG}
authority:
  namespaceTag: ${AUTHNAMESPACETAG}
domainSuffix: ${DOMAINSUFFIX}

argocd:
  appname: ${NAMESPACE}
  namespace: argocd

cluster:
  address: https://kubernetes.default.svc
  namespace: ${NAMESPACE}
  commonToolsNamespace: ${COMMONNAMESPACE}
  issuer: dev-prod

hashicorp:
  service: "http://vault.namespace.svc.cluster.local:8200"
  secretEngine: ${SECRETENGINE}
  role: dev-int-role

values:
  repo_URL: https://code.europa.eu/simpl/simpl-open/development/agents/data-provider.git
  branch: develop

postgresql:
  repo_URL: https://charts.bitnami.com/bitnami
  chart_name: postgresql
  targetRevision: 15.2.5
  nameOverride: postgresql
  primary:
    resourcesPreset: "none"
    resources:
      requests:
        cpu: "0"
        memory: "0"
  valueFiles:
    - values.yaml
    - values-participant.yaml

keycloak:
  repo_URL: https://charts.bitnami.com/bitnami
  chart_name: keycloak
  targetRevision: 22.2.5
  nameOverride: keycloak
  resourcesPreset: "none"
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml

redis:
  repo_URL: 'https://charts.bitnami.com/bitnami'
  targetRevision: 19.6.0
  chart_name: redis
  nameOverride: redis
  master:
    resourcesPreset: "none"
    resources:
      requests:
        cpu: "0"
        memory: "0"
  valueFiles:
    - values.yaml
    
signer:
  projectID: "954"
  targetRevision: 0.0.2
  chart_name: signer
  serviceAccountName: signer-service-account
  valueFiles:
    - values.yaml

simpl_cloud_gateway:
  projectID: "772"
  targetRevision: 1.0.0
  chart_name: simpl-cloud-gateway
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml
    - values-routes.yaml

sdtooling_api_be:
  projectID: "778"
  targetRevision: 1.5.1
  chart_name: sd-creation-wizard
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml
  nameOverride: sd

sdtooling_validation_api_be:
  projectID: "868"
  targetRevision: 1.3.0
  chart_name: sd-creation-wizard
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml
  nameOverride: sd

xsfc_advsearch_be:
  projectID: "891"
  targetRevision: 1.4.1
  chart_name: xsfc-advsearch
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml
  nameOverride: xsfc-advsearch-be

users_roles:
  projectID: "771"
  targetRevision: 1.0.0
  chart_name: users-roles
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml

simpl_fe:
  projectID: "769"
  targetRevision: 1.0.7
  chart_name: simpl-fe
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml
    - values-participant.yaml

sd_ui:
  projectID: "923"
  targetRevision: 1.0.0
  chart_name: sd-ui
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml

infrastructure_be:
  projectID: "896"
  targetRevision: 0.2.1
  chart_name: infrastructure-be
  fullnameOverride: infrastructure-be
  valueFiles:
    - values.yaml

infrastructure_fe:
  projectID: "895"
  targetRevision: 0.1.9
  chart_name: frontend
  fullnameOverride: infrastructure-fe
  valueFiles:
    - values.yaml


tls_gateway:
  projectID: "860"
  targetRevision: 1.0.1
  chart_name: tls-gateway
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml
    - values-routes.yaml

simpl_edc:
  projectID: "920"
  targetRevision: 1.0.1
  chart_name: edc
  nameOverride: simpl-edc
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml
    - values-provider.yaml

simpl_catalogue_client:
  projectID: "937"
  targetRevision: 1.0.0
  chart_name: catalogue-ui
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml

auth_provider:
  projectID: "939"
  targetRevision: 1.0.0
  chart_name: authentication-provider
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml

monitoring:
  enabled: true
  projectID: "828"
  targetRevision: 0.1.11
  chart_name: eck-monitoring
  fullnameOverride: elastic
  valueFiles:
    - values.yaml

contract:
  projectID: "872"
  targetRevision: 1.0.1
  chart_name: simpl-contract
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml   
  fullnameOverride: contract-dataprovider 

stubs:
  projectID: "930"
  targetRevision: 1.0.0
  chart_name: simpl-stubs
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml  

files:
  projectID: "978"
  targetRevision: 0.0.2
  chart_name: simpl-files
  nameOverride: simpl-files
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml  

crossplane:
  enabled: false
  repo_URL: https://charts.crossplane.io/stable
  targetRevision: 1.16.2
  chart_name: crossplane
  resourcesCrossplane:
    requests:
      cpu: "0"
      memory: "0"
  resourcesRBACManager:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml

gitea:
  repo_URL: https://dl.gitea.com/charts
  targetRevision: 10.4.1
  chart_name: gitea
  nameOverride: gitea
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml

argocd:
  repo_URL: https://argoproj.github.io/argo-helm
  targetRevision: 6.7.13
  chart_name: argo-cd
  nameOverride: infrastructure-argo-cd
  valueFiles:
    - values.yaml

argoevents:
  repo_URL: https://argoproj.github.io/argo-helm
  targetRevision: 2.4.8
  chart_name: argo-events
  nameOverride: infrastructure-argo-events
  valueFiles:
    - values.yaml

argoworkflows:
  repo_URL: https://argoproj.github.io/argo-helm
  targetRevision: 0.42.5
  chart_name: argo-workflows
  nameOverride: infrastructure-argo-workflows
  valueFiles:
    - values.yaml

provisionerresources:
  projectID: "912"
  targetRevision: 0.1.0
  chart_name: infrastructure-crossplane
  nameOverride: infrastructure-crossplane
  valueFiles:
    - values.yaml
