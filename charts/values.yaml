project: ${PROJECT}
namespaceTag: ${NAMESPACETAG}
authority:
  namespaceTag: ${AUTHNAMESPACETAG}
domainSuffix: ${DOMAINSUFFIX}

argocd:
  appname: ${NAMESPACE}
  namespace: argocd

cluster:
  address: https://kubernetes.default.svc
  namespace: ${NAMESPACE}
  commonToolsNamespace: ${COMMONNAMESPACE}

secretEngine: ${SECRETENGINE}
hashicorp:
  service: "http://vault-ha.vault-ha.svc.cluster.local:8200"

values:
  repo_URL: https://code.europa.eu/simpl/simpl-open/development/agents/data-provider.git
  branch: develop

postgresql:
  repo_URL: https://charts.bitnami.com/bitnami
  chart_name: postgresql
  targetRevision: v15.2.5
  nameOverride: postgresql
  primary:
    resourcesPreset: "none"
    resources:
      requests:
        cpu: "0"
        memory: "0"
  valueFiles:
    - values.yaml
    - values-participant.yaml

keycloak:
  repo_URL: https://charts.bitnami.com/bitnami
  chart_name: keycloak
  targetRevision: "22.2.5"
  nameOverride: keycloak
  resourcesPreset: "none"
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml

redis:
  repo_URL: 'https://charts.bitnami.com/bitnami'
  targetRevision: 19.6.0
  chart_name: redis
  nameOverride: redis
  master:
    resourcesPreset: "none"
    resources:
      requests:
        cpu: "0"
        memory: "0"
  valueFiles:
    - values.yaml
    
signer:
  projectID: "954"
  targetRevision: 0.0.2
  chart_name: signer
  serviceAccountName: signer-svc-account-iaa
  valueFiles:
    - values.yaml

simpl_cloud_gateway:
  projectID: "772"
  targetRevision: 0.8.0
  chart_name: simpl-cloud-gateway
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml
    - values-routes.yaml

sdtooling_api_be:
  projectID: "778"
  targetRevision: v1.4.0-rc.445
  chart_name: sd-creation-wizard
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml
  nameOverride: sd

sdtooling_validation_api_be:
  projectID: "868"
  targetRevision: 1.2.0
  chart_name: sd-creation-wizard
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml
  nameOverride: sd

xsfc_advsearch_be:
  projectID: "891"
  targetRevision: v1.3.0-rc.119
  chart_name: xsfc-advsearch
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml
  nameOverride: xsfc-advsearch-be

users_roles:
  projectID: "771"
  targetRevision: 0.8.1
  chart_name: users-roles
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml

simpl_fe:
  projectID: "769"
  targetRevision: 0.8.0
  chart_name: simpl-fe
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml
    - values-participant.yaml

sd_ui:
  projectID: "923"
  targetRevision: 0.3.1
  chart_name: sd-ui
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml

tls_gateway:
  projectID: "860"
  targetRevision: 0.8.3
  chart_name: tls-gateway
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml
    - values-routes.yaml

simpl_edc:
  projectID: "920"
  targetRevision: 0.0.7-SNAPSHOT.243.8e6f819d
  chart_name: edc
  nameOverride: simpl-edc
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml
    - values-provider.yaml

simpl_catalogue_client:
  projectID: "937"
  targetRevision: 0.2.1
  chart_name: charts
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml

auth_provider:
  projectID: "939"
  targetRevision: 0.8.1
  chart_name: authentication-provider
  resources:
    requests:
      cpu: "0"
      memory: "0"
  valueFiles:
    - values.yaml

monitoring:
  enabled: true
  projectID: "828"
  targetRevision: 0.1.7
  chart_name: eck-monitoring
  fullnameOverride: elastic
  valueFiles:
    - values.yaml
    - values_other.yaml